package Div_2_668

import java.util.*
import kotlin.collections.ArrayList

fun main() {

    /*val scan = Scanner(System.`in`)
    //val s = scan.nextLine().toString()
    var t = scan.nextLine().toInt()
    while (t > 0) {
        t--
        var n = scan.nextLine().toInt()
        var arr = scan.nextLine().split(" ")
        arr.reversed().forEach {
            print("$it ")
        }
        println()
*/

}